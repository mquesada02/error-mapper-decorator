{
  "name": "error-mapper-decorator",
  "version": "0.2.0",
  "description": "Tiny, type-safe error-mapping decorator",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "test:runtime": "tsc -p test/integration/legacy/tsconfig.json && node test/integration/legacy/dist/index.js && tsc -p test/integration/stage3/tsconfig.json && node test/integration/stage3/dist/index.js",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format --write .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:legacy": "tsc -p tsconfig.legacy.json --noEmit",
    "publint": "publint",
    "attw": "attw --pack",
    "verify": "pnpm lint && pnpm typecheck && pnpm typecheck:legacy && pnpm coverage && pnpm build && pnpm test:runtime && pnpm publint && pnpm attw",
    "prepublishOnly": "pnpm verify"
  },
  "keywords": [
    "decorator",
    "error",
    "error-handling",
    "error-mapping",
    "exception",
    "typescript",
    "experimentalDecorators",
    "stage-3"
  ],
  "author": "mquesada",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mquesada02/error-mapper-decorator.git"
  },
  "bugs": {
    "url": "https://github.com/mquesada02/error-mapper-decorator/issues"
  },
  "homepage": "https://github.com/mquesada02/error-mapper-decorator#readme",
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18",
    "@biomejs/biome": "^2",
    "@types/node": "^22",
    "@vitest/coverage-v8": "^3",
    "publint": "^0.3",
    "reflect-metadata": "^0.2",
    "tsup": "^8",
    "typescript": "^5",
    "vitest": "^3"
  }
}
